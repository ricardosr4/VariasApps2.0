<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VariasApp20"
        tools:targetApi="31">
        <activity
            android:name=".datePicker.DatePicker"
            android:exported="false" />
        <activity
            android:name=".timePicker.TimePicker"
            android:exported="false" />
        <activity
            android:name=".IMC.ResultIMC"
            android:exported="false" />
        <activity
            android:name=".IMC.IMC"
            android:exported="false" />
        <activity
            android:name=".menu.Menu"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".saludo.ResultActivity"
            android:exported="false" />
        <activity
            android:name=".saludo.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
    </application>

</manifest>